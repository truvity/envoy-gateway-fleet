fleets:
  internal:
    additionalServices:
      invalid:
        type: NodePort
        ports:
          - {name: http, port: 80, targetPort: 10080, nodePort: 32080}
          - {name: https, port: 443, targetPort: 10443, nodePort: 32080}

fleets:
  internal:
    listeners:
      test:
        hostname: api.example.com
        certificate: {enabled: false}
        allowedRoutes:
          namespaces:
            from: All
        infraHealth:
          enabled: true

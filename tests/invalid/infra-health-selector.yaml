fleets:
  internal:
    listeners:
      test:
        hostname: api.example.com
        certificate: {enabled: false}
        allowedRoutes:
          namespaces:
            from: Selector
            selector:
              matchLabels:
                kubernetes.io/metadata.name: selected
        infraHealth:
          enabled: true

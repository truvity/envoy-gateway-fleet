apiVersion: v2
name: envoy-gateway-fleet
description: >-
  Envoy Gateway fleets — per-audience GatewayClass + EnvoyProxy
  (mergeGateways: one Envoy fleet, one stable Service per class), an
  optional health listener with its cert-manager Certificate, and the
  fleet's NetworkPolicy. The controller itself is upstream's chart.
type: application
# Placeholder: the git tag is the sole version authority. The release
# workflow stamps both fields from the tag (truvity/ci-workflows
# release-public.yaml).
version: 0.0.0
appVersion: 0.0.0
home: https://github.com/truvity/envoy-gateway-fleet
sources:
  - https://github.com/truvity/envoy-gateway-fleet
keywords:
  - envoy-gateway
  - gateway-api
maintainers:
  - name: Truvity
    url: https://github.com/truvity
